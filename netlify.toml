# Netlify build & deploy configuration
[build]
  command = "npm install && npm run build"
  publish = "dist"

# SPA routing for React Router (client-side routes)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200